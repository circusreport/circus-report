{
  "name": "news-aggregator-bot",
  "version": "1.0.0",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.64.0",
    "cloudinary": "^1.41.0",
    "axios": "^1.6.0",
    "express": "^4.18.2"
  }
}
```

5. Click **Commit new file**

---

### File 2: Procfile

This tells Railway exactly how to start your bot.

1. **Add file** → **Create new file**
2. Name it exactly: `Procfile` (capital P, no extension)
3. Paste this:
```
web: node bot.js
